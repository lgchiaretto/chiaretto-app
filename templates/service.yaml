apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ Values.release.name }}
    app.kubernetes.io/component: {{ Values.release.name }}
    app.kubernetes.io/instance: {{ Values.release.name }}
  name: {{ Values.release.name }}
spec:
  ports:
  - name: 8081-tcp
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    deploymentconfig: {{ Values.release.name }}
  sessionAffinity: None
  type: ClusterIP
