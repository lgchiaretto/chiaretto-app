apiVersion: v1
data:
  APP_VERSION: "{{ .Values.configmap }}"
kind: ConfigMap
metadata:
  name: chiaretto-app-version
---
apiVersion: v1
data:
  APP_ENV: "{{ .Values.env }}"
kind: ConfigMap
metadata:
  name: chiaretto-app-environment

